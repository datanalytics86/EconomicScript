name: CI

on:
  push:
    branches: [main, "claude/**"]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 8 * * 1"   # cada lunes a las 8am UTC

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Instalar dependencias
        run: pip install -r requirements.txt

      - name: Inicializar base de datos de prueba
        run: python -c "from db import Database; Database('test.db').init_schema()"
        env:
          DB_PATH: test.db

      - name: Ejecutar tests
        run: pytest tests/ -v --tb=short --cov=utils --cov-fail-under=80
        env:
          DB_PATH: test.db
